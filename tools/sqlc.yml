version: 1
packages:
  - path: "../linkgraph/db/cockroach/sqlc"
    name: "sqlc"
    engine: "postgresql"
    schema: "../linkgraph/db/cockroach/migrations"
    queries: "../linkgraph/db/cockroach/queries"
    emit_exact_table_names: true
    emit_interface: true
    emit_json_tags: true